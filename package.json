{
  "name": "codesage-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npx prisma generate --schema=client/prisma/schema.prisma && npm --prefix client run build",
    "start": "npm --prefix client run start"
  }
}
