{
  "name": "codesage-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "cd client && npm install --legacy-peer-deps && npx prisma@6.16.2 generate && npm run build",
    "start": "cd client && npm start",
    "postinstall": "cd client && npm install --legacy-peer-deps && npx prisma@6.16.2 generate"
  },
  "dependencies": {
    "@prisma/client": "6.16.2"
  },
  "devDependencies": {
    "prisma": "6.16.2"
  }
}
