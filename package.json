{
  "name": "codesage-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "cd client && npm install --legacy-peer-deps && npx prisma generate && npm run build",
    "start": "cd client && npm start"
  }
}
